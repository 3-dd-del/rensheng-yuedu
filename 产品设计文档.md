# 人生阅读 · MVP 产品设计（初稿）

- 版本：v0.1
- 日期：2026-09-08
- 状态：待评审

## 1. 产品定位

人生阅读是一款「本地优先」的个人电子阅读器。MVP 阶段只做一件事：**导入 TXT 书籍，读得动大书，记得住进度**，让用户随时放下、随时接着读。

MVP 核心闭环：导入书籍 → 书架管理 → 开始阅读 → 记录进度 → 下次续读

MVP 成功标准：

- 常见中文 TXT 能顺利导入，不乱码；
- 数百万字级别的大文件打开快、阅读流畅、不卡顿；
- 关闭后重新打开，能准确回到上次阅读位置；
- 全程本地使用，不需要账号和网络。

## 2. 产品形态（已确认）

采用「本地网页版」：浏览器打开即用，书籍、进度与设置全部保存在本机，无需账号与网络。

已确认的产品要求：

- 以浏览器为运行环境，MVP 不做 Electron/Tauri，也不发布安装包；
- 阅读器内置键盘快捷键，常用操作尽量不依赖鼠标即可完成；
- 提供桌面入口：双击桌面图标即可打开默认浏览器并直达阅读器；
- 阅读页采用「翻页式」阅读（非连续滚动式），并支持进度条拖拽跳转。

选择理由：开发与迭代最快，无需安装和分发流程；技术方案成熟；后续若做桌面版或移动端，产品设计与核心逻辑可复用，只需替换外壳（届时可套用 Electron/Tauri）。

## 3. 功能范围

### 3.1 第一版必做

| 模块 | 说明 |
| --- | --- |
| 书籍导入 | 支持 TXT 文件（选择或拖拽）；自动识别编码避免乱码，识别失败时手动指定并可预览；导入后由应用自己保管文件，不依赖原始路径；重复导入按内容指纹识别，书库已存在时提示直达该书，不产生重复条目 |
| 书架 | 展示所有书：书名、阅读进度；最近在读的排前面；支持删除书籍；提供一键「继续阅读」 |
| 阅读页 | 翻页式长文本流畅阅读，用键盘翻页或点击翻页，非滚动式；固定亮色主题；字号、行距可调；底部进度条显示百分比，可拖动跳转 |
| 断点续读 | 本地记录精确阅读位置；打开书自动回到上次位置 |
| 阅读设置 | 字号、行距偏好本地记忆，全局生效 |
| 键盘快捷键 | 常用操作（翻页、返回书架、打开阅读设置等）可用键盘完成，并提供快捷键说明 |
| 桌面入口 | 提供桌面快捷方式，双击后用默认浏览器打开阅读器 |

### 3.2 第一版不做（后续迭代）

账号与云同步、多设备、EPUB/PDF、书签与划线笔记、章节目录、暗色模式、全文搜索、词典翻译、听书、阅读统计、AI 辅助、书城。

## 4. 关键质量要求

以下两项不是附加功能，而是「阅读器成立」的必要条件。

### 4.1 中文编码识别

- 常见中文 TXT 并非只有 UTF-8，大量老资源是 GBK/GB18030 编码，直接按 UTF-8 读会出现乱码；
- 导入时自动识别编码（含 BOM 判断与内容判断），识别失败时提供手动选择与预览；
- 导入后统一为内部存储格式，后续阅读不再受原文件编码影响。

### 4.2 大文件阅读流畅度

- 真实长篇小说通常为 1–10MB（几十万到几百万字），必须按这个量级设计，而不是按小文件设计；
- 不能一次性把整本书渲染进界面，打开速度、滚动/翻页手感不能随文件增大明显变差；
- 阅读位置要按字符偏移精确记录，保证大文件下也能准确续读；
- 这是第一版最重要的技术任务，优先级等同任何功能。

## 5. 界面结构（初稿）

1. 书架页（默认首页）：导入入口、书籍列表（书名 + 进度）、「继续阅读」入口
2. 阅读页：正文、返回书架、进度显示与跳转、阅读设置入口
3. 导入流程：选择文件 → 编码识别/确认 → 导入完成

## 6. 数据说明

- 全部数据保存在本机，不上传；
- 书：书名、原文件名、内部文件、总字符数、上次阅读位置（精确偏移）、最近阅读时间；
- 阅读设置：字号、行距。

## 7. 后续迭代方向

- 体验增强：书签、划线、笔记、暗色模式、全文搜索；
- 格式扩展：EPUB（届时再引入真实章节模型）、PDF；
- 产品化：多端同步、阅读统计、听书、AI 阅读辅助、书城。

## 8. 决策记录与待确认

已确认（2026-09-08）：

1. 产品形态为本地网页版，在浏览器中使用，不打包桌面应用；
2. 阅读页采用翻页式阅读；
3. 阅读器内置键盘快捷键；
4. 提供桌面一键打开入口（双击桌面图标 → 默认浏览器打开阅读器）；
5. 重复导入按内容指纹识别：检测到同一文件已存在时提示并直达该书，不产生重复条目。

仍在讨论：

- 书名相同但内容不同的不同版本，是否需要在书架上合并展示或允许共存；
- 翻页动效、视觉样式等交互细节。
